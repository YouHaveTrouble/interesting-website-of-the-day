---
export const prerender = false;

import Website from "../models/Website";

const website = await Website.getRandomWebsite();

if (website !== null) {
  return Astro.redirect(website.url);
}

Astro.response.status = 500;

---
